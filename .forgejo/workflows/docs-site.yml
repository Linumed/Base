name: docs-site

# Builds the MkDocs operations handbook and publishes it as a generic package - the
# artifact Website#5 pulls from to host it at linumed.com/base/docs/ (issue #55).
#
# Deliberately stops at "publish an artifact", not "deploy it". Actually placing the
# built HTML on linumed.com is a manual pull by cc@prod, same as every other prod change
# - "Kein Auto-Deploy. Prod-Deploy ist ein manueller Gate" (CONVENTIONS.md). This workflow
# only replaces the manual `mkdocs build && tar czf && curl ...` steps that were
# previously run by hand on every doc change (found tedious and error-prone in practice -
# a stale checksum was nearly handed over once, see issue #55's follow-up).
#
# Publishes to a fixed package version ("latest") rather than a fresh one every run:
# cc@prod can always pull the same URL and get whatever is current. This used to be a
# moving Forgejo release tag instead - dropped in issue #85, because the push mirror
# copies every git tag to the public GitHub repository, and a build artifact's tag ended
# up sitting in the public tag list next to real releases (v0.4.0 etc.), pointing at
# nothing there once mirrored (the mirror carries tags, not release assets). A package
# has no git ref at all and cannot leak that way.
on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - 'README.md'
      - 'ARCHITECTURE.md'
      - 'CHANGELOG.md'
      # The workflow's own file, so a change to the doc build is exercised by the doc
      # build. Without this it was the one edit that never triggered its own test - the
      # 2026-08-21 tag fix (issue #78) had to be dispatched by hand to be verified at all.
      # Same gap as #62, one level up: the thing that tests everything else tested every
      # change but its own.
      - '.forgejo/workflows/docs-site.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  docs-site:
    runs-on: docker
    # PACKAGE_TOKEN is a Forgejo token with write:package, created by the account that
    # owns this repository and stored as a repo secret - nothing in this repository
    # should mint or print it. `permissions: packages: write` does not work here: measured
    # 2026-08-22, the Actions run token is rejected by the package registry with 401
    # (`reqPackageAccess`) regardless, and separately, packages live in the owning
    # account's namespace - a token from a mere collaborator account got 401 too, only a
    # token from the repo's actual owner account worked.
    steps:
      - uses: actions/checkout@v4

      # Pinned to what's known to build cleanly (verified 2026-08-19) - an unpinned
      # `pip install mkdocs mkdocs-material` would let an upstream release turn this
      # workflow red (or worse, green-but-different) without a deliberate version bump.
      - name: Install MkDocs and Material theme
        run: |
          apt-get update -qq
          apt-get install -y -qq --no-install-recommends python3-pip curl
          pip install --no-cache-dir --break-system-packages mkdocs==1.6.1 mkdocs-material==9.7.7

      # --strict turns the three link/resource bugs found in #55 (dead ADR links, an
      # external Google Fonts request, dead ROADMAP issue links) into a hard failure
      # instead of a warning nobody reads - if a future doc change reintroduces a dead
      # link, this job fails instead of silently shipping it.
      - name: Build with mkdocs build --strict
        run: mkdocs build --strict --site-dir site

      - name: Package the built site
        id: package
        run: |
          tar czf base-docs-site.tar.gz -C site .
          echo "sha256=$(sha256sum base-docs-site.tar.gz | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"

      # The migration from a moving release tag to the package registry (issue #85) is
      # done: cc@prod switched over and confirmed the pull independently (2026-08-22), and
      # the old release/tag were deleted on both Forgejo and the GitHub mirror. This is
      # now the only publish path, so a missing secret is a hard failure, not a silent
      # fallback - the fallback existed only for the migration window.
      - name: Preflight - abort if PACKAGE_TOKEN is not set
        env:
          PACKAGE_TOKEN: ${{ secrets.PACKAGE_TOKEN }}
        run: |
          if [ -z "${PACKAGE_TOKEN}" ]; then
            echo "::error::PACKAGE_TOKEN is not set. Needs a Forgejo token with write:package," >&2
            echo "created by the account that owns this repository (a collaborator token is" >&2
            echo "not enough - packages live in the owner account's namespace), stored as a" >&2
            echo "repo secret named PACKAGE_TOKEN." >&2
            exit 1
          fi

      # The registry rejects a re-upload of the same version+filename, so the version is
      # deleted first. A 404 there is the expected answer on the very first run.
      - name: Publish the artifact as a generic package
        env:
          GITEA_TOKEN: ${{ secrets.PACKAGE_TOKEN }}
        run: |
          PKG="${{ github.server_url }}/api/packages/${{ github.repository_owner }}/generic/base-docs-site/latest"
          curl -s -X DELETE -H "Authorization: token ${GITEA_TOKEN}" "${PKG}" || true
          sha256sum base-docs-site.tar.gz | cut -d' ' -f1 > base-docs-site.tar.gz.sha256
          for f in base-docs-site.tar.gz base-docs-site.tar.gz.sha256; do
            code=$(curl -s -o /dev/null -w '%{http_code}' -X PUT \
              -H "Authorization: token ${GITEA_TOKEN}" \
              --upload-file "$f" "${PKG}/$f")
            echo "PUT $f -> $code"
            case "$code" in 2*) ;; *) echo "::error::uploading $f to the package registry returned $code" >&2; exit 1;; esac
          done

      # Publishing is not the same as being fetchable, and this repository has shipped
      # that gap before (#78, where a release tag never moved while its asset changed).
      - name: Assert the package is fetchable and matches what was built
        env:
          GITEA_TOKEN: ${{ secrets.PACKAGE_TOKEN }}
        run: |
          PKG="${{ github.server_url }}/api/packages/${{ github.repository_owner }}/generic/base-docs-site/latest"
          curl -fsSL -H "Authorization: token ${GITEA_TOKEN}" "${PKG}/base-docs-site.tar.gz" -o fetched.tar.gz
          echo "$(cat base-docs-site.tar.gz.sha256)  fetched.tar.gz" | sha256sum -c -
          rm -f fetched.tar.gz

