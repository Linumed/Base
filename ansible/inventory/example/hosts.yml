---
# Example inventory. Never real hosts - copy this outside the repo and fill in your own.
all:
  children:
    linumed:
      hosts:
        linumed-example-01:
          ansible_host: 203.0.113.10
          ansible_user: admin
