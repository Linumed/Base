---
galaxy_info:
  role_name: bridgelink
  author: Linumed
  description: HL7 v2 integration engine for Linumed Base (BridgeLink, an MPL-2.0 fork of Mirth Connect, with PostgreSQL)
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
  # No `fhir` tag: the engine ships no FHIR data type (issue #97) - a tag people search
  # by is a promise like any other line of documentation.
  galaxy_tags:
    - hl7
    - healthcare
    - integration
    - bridgelink

dependencies: []
