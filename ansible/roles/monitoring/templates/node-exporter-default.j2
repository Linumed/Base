# Managed by Ansible (linumed-os, role: monitoring). Do not edit by hand, changes will be
# overwritten. Not a ucf drop-in mechanism like sshd - this file only ever contained a
# single ARGS= line, so a full-file template is safe and doesn't fight the package on
# upgrade the way overwriting sshd_config would.
ARGS="--web.listen-address=:{{ monitoring_node_exporter_port }} --collector.textfile.directory={{ monitoring_node_exporter_textfile_dir }}"
